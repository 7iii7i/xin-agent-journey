# 2026-09-06（第 1 天）

## 做了什么

- 装好环境：Python 3.13.14、uv 0.12.10、Git 2.55
- `uv init` 建了项目骨架，venv 建好，装了 requests + python-dotenv
- 写了 `week01/day1_first_call.py`，跑通离线演示模式

## 今天搞懂的

一次 API 调用 = 同一个数据不停变形态：

```
str（你的想法）
  → list[dict]（messages）
  → dict（payload，加模型/温度）
  → str（requests 偷偷 json.dumps，网络只能传文本）
  → dict（response.json() 还原）
  → str（从嵌套里掏出 content）
```

最容易卡人的地方：`requests.post(url, json=xxx)` 里的 `json=` 参数
**自动帮你做了 json.dumps()**。没写 dumps 却能用，就是它在背后干的。

## 还没搞懂 / 卡住的地方

（每天写完代码在这里记，不要等到忘了才补）

-

## 第二天（2026-09-06 晚）

忻反馈：两天前（2026-09-04）已用 OpenAI SDK 跑通过多轮对话机器人，但"语法不熟、不能融会贯通"。
诊断：懂流程、不熟语法 —— 不是不会，是练习不够。
处方：已存两份对照代码进项目，明天手动重写。

- `week01/day2_chatbot_sdk.py`：他找到的 SDK 版本，作为对照
- `week01/day2_chatbot_requests.py`：用 requests 重写版，每行注释解释 SDK 替你做了啥

明天任务：合上 requests 版，从头敲一遍跑通，再对比。

## 明天做

1. ~~注册 DeepSeek~~ ✅ 已完成
2. ~~跑真实调用~~ ✅ 已完成
3. 手动用 requests 重写 day2_chatbot_sdk.py
4. 在重写版里加上 finish_reason 判断（不超过 3 行）
5. 把 README 里"我为什么转行"那段写了

## 状态

时长：约 2 小时（含跑通真实调用 + 写对照代码）
