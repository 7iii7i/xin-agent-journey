# Python 学习总词表（W1–W2，截至 2026-09-09）

> 用法：英文词 → 中文名 → 干啥用。回看这张表 = 回看你从零基础到现在学过的全部"工具"。
> 配套错题本：`week02/notes/day2_stdlib_cheatsheet.md`（16 条易混点）

---

## 一、W1 地基：变量 / 类型 / 容器

| 英文 / 写法 | 中文 | 干啥用 |
|---|---|---|
| `变量 = 值` | 赋值 | 给数据起个名字存起来，以后用名字就能拿到值 |
| `int` | 整数 | 整数类型，如 `5` `-3` |
| `float` | 浮点数 | 小数类型，如 `3.14` |
| `str` | 字符串 | 文字类型，用引号包起来 `"苹果"` |
| `bool` | 布尔 | 真/假 `True` / `False`，用于判断 |
| `list` `[ ]` | 列表 | 有序容器，可改，放一堆数据 `[1,2,3]` |
| `dict` `{ }` | 字典 | 键值对容器 `{"name": "楚尘"}`，按键取值 |
| `tuple` `( )` | 元组 | 有序容器，建好不能改 |
| `set` `{ }` | 集合 | 去重容器，不管顺序 |
| `for ... in ...` | for 循环 | 逐个取出容器里的元素处理 |
| `while` | while 循环 | 条件为真就一直重复 |
| `if / elif / else` | 条件判断 | 满足不同条件走不同分支 |
| 缩进（4 空格） | 缩进 | Python 用缩进表示"谁属于谁"，不能乱 |
| `def` | 定义函数 | 把一段功能打包，起名后能反复调用 |
| `return` | 返回 | 函数算完把结果交出来 |
| 函数调用 `func()` | 调用 | 用名字触发函数执行 |
| 参数 `def f(x)` | 参数 | 调用时传给函数的输入 |
| `class` | 类 | 造"模板"，批量造同类对象 |
| 对象 / 实例 | 实例 | 用类造出来的具体东西 |
| `__init__` | 构造方法 | 创建对象时自动跑，初始化属性 |
| `self` | 自身 | 类里指代"当前这个对象" |
| 方法 `def 方法(self)` | 方法 | 类里的函数，对象能干的动作 |
| `if __name__ == "__main__":` | 门卫 | 只有直接运行本文件才执行，被导入时不跑 |

## 二、列表方法 + lambda（W1 进阶）

| 英文 / 写法 | 中文 | 干啥用 |
|---|---|---|
| `len(x)` | 长度 | 数出容器有几个元素 |
| `in` | 在不在 | 判断某元素是否在容器里，返回 True/False |
| `append(x)` | 追加 | 往列表末尾加一个元素 |
| `remove(x)` | 删除 | 从列表删掉某个元素 |
| `sorted(x)` | 排序 | 返回排好序的新列表（不破坏原列表） |
| `min(x)` / `max(x)` | 最小/最大 | 取容器里最小/最大的元素 |
| `lambda x: ...` | 匿名函数 | 临时写个小函数，常配合 sorted 当排序键 |
| `dict.get(k)` | 安全取值 | 字典取值，键不在也不报错（可给默认值） |

## 三、文件读写（W1）

| 英文 / 写法 | 中文 | 干啥用 |
|---|---|---|
| `open(路径, mode)` | 打开文件 | 开文件柜的门；不写 mode 默认 `"r"` 只读 |
| `mode "r"/"w"/"a"` | 模式 | 只读 / 写入(清空) / 追加 |
| `with open(...) as f:` | 自动关文件 | 用完自动关门，不用手动 close |
| `write("文字")` | 写 | 往文件写一段文字 |
| `read()` / `readlines()` | 读 | 读全部 / 按行读成列表 |
| `strip()` | 去空白 | 去掉字符串首尾换行/空格 |
| `encoding="utf-8"` | 编码 | 规定文字↔字节怎么翻，中文文件必写 |
| `read_text()` / `write_text()` | pathlib 读写 | Path 对象的读写文件方法（不用 open） |

## 四、标准库（W2，今天核心）

| 英文 / 写法 | 中文 | 干啥用 |
|---|---|---|
| `import` | 导入 | 把标准库/第三方库请进来用 |
| `json` | JSON 模块 | 字典 ↔ JSON 文本 的翻译员 |
| `json.dump(字典, f)` | 存文件 | 字典 → 写进文件（一步到位） |
| `json.load(f)` | 读文件 | 文件 → 读回字典 |
| `json.dumps(字典)` | 变字符串 | 字典 → 内存里一段 JSON 字符串（不碰磁盘） |
| `json.loads(字符串)` | 字符串变字典 | JSON 字符串 → 读回字典 |
| `ensure_ascii=False` | 中文原样 | 让中文不转成 \uXXXX 乱码 |
| `os` | 系统模块 | 管文件/路径等系统操作 |
| `os.path.exists(路径)` | 存在判断 | 文件/文件夹在不在，返回 True/False |
| `datetime` | 日期时间模块 | 管时间 |
| `datetime.now()` | 当前时间 | 取此刻时间 |
| `strftime("%Y-%m-%d %H:%M:%S")` | 时间格式化 | 把时间按模具变成字符串；文件名只用 `%Y-%m-%d`（无冒号） |
| `pathlib` | 路径模块 | 跨平台管文件路径 |
| `Path("a") / "b.txt"` | 拼路径 | `/` 被重定义为拼路径，自动适配系统斜杠 |
| `Path.mkdir(parents=True, exist_ok=True)` | 建目录 | 建文件夹，连上级一起建、已存在不报错 |
| `p.parent` | 父目录 | 取文件的上级文件夹（建目录前先用它） |
| `requests` | 网络请求库 | 下周调大模型 API 用（今天只认脸） |

## 五、格式与易混概念（今天踩坑总结）

| 英文 / 写法 | 中文 | 干啥用 / 要点 |
|---|---|---|
| `f"记录_{变量}"` | f-string | 把变量值塞进字符串；引号里写变量名只是死字 |
| `+` 拼接 | 加号拼接 | `"a" + 变量` 把文字粘成一段（结果还是一个参数） |
| `,` 分隔 | 逗号分隔 | 分隔函数的多个参数，只适合 `print`，写文件会报错 |
| 变量先定义后使用 | 顺序 | Python 从上往下读，用之前必须先定义 |
| `print(x)` | 整体打印 | 举起来看整体，单个对象不用 for |
| `for` | 逐个处理 | 只想把多个元素逐个拆开处理时才用 |
| JSON | JSON 格式 | 一种用纯文本表示数据的通用格式，≠ 文件本身 |
| `Path()` 不建文件 | 路径对象 | 只是路牌/指路牌，真正建文件是 `write_text` 那步 |

## 六、异常处理（用户 23:46 回忆已懂 else 档）

| 英文 / 写法 | 中文 | 干啥用 |
|---|---|---|
| `try:` | 尝试 | 把"可能报错的代码"放这里，先试着跑 |
| `except 异常:` | 兜底 | try 里**报错**才执行；可指定具体异常（如 `except FileNotFoundError:` 只兜"文件不存在"），不写类型则兜所有错 |
| `else:` | 成功分支 | try 里**没报错**才执行（用户已正确掌握：try 成功就走 else） |
| `finally:` | 必跑 | 无论 try 报错与否**都执行**，常用于关文件/清理 |

---

**进度定位**：以上 = 计划「阶段 1 · Python 地基 W1–2」全部内容（含 try/except 概念已懂）。下一项待补：①try/except **单练写一遍**（概念懂但未手写过）②W2 毕业小工具（读JSON→过滤→写入，带异常处理+`__main__`门卫）③自己敲 git 命令。做完即进 W3 调大模型 API。
